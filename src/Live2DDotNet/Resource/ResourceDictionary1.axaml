<ResourceDictionary
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:icon="using:Live2DDotNet.Utils"
    xmlns:vm="using:Live2DDotNet.UI.Model.Items">
    <DataTemplate x:Key="FileNameCell1" DataType="vm:FileTreeNodeModel">
        <CheckBox IsChecked="{Binding IsChecked, Mode=TwoWay}" />
    </DataTemplate>
    <DataTemplate x:Key="FileNameCell" DataType="vm:FileTreeNodeModel">
        <StackPanel Orientation="Horizontal">
            <TextBlock
                Width="30"
                HorizontalAlignment="Center"
                TextAlignment="Center">
            </TextBlock>
            <TextBlock Text="{Binding Name}" />
        </StackPanel>
    </DataTemplate>
    
</ResourceDictionary>
